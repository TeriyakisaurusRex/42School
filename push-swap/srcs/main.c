#include "push_swap.h"

int main(int argc, char **argv)
{
    //check argv
    //error_catch
    //if all clear then convert argv to stack a
    return (0);
}

int* input_to_intput (char **argv)
{
    int i;
    int j;
    int num_args;
    int *list;

    num_args = count_args();
    while (argv[i][j])
    {
        while (ft_isdigit(argv[i][j]))
            j++; //change to if that brakes
        temp = ft_atoi(argv[i])
        //read
        i++;
    }
}

int assign_to_stack 